# pharmalens/requirements.txt

# ── core AI ────────────────────────────────────────────────────────────────────
# Unified Google GenAI SDK — covers Gemini via Vertex AI
# Note: google-generativeai and google-cloud-aiplatform genai modules
# are deprecated as of Nov 2025 — use this instead
google-genai>=1.10.0

# ── orchestrator ───────────────────────────────────────────────────────────────
schedule>=1.2.0          # daily/weekly job scheduling in orchestrator.py

# ── environment / config ───────────────────────────────────────────────────────
python-dotenv>=1.0.0     # load .env file so credentials stay out of code

# ── reference data build (one-time script only) ────────────────────────────────
pandas>=2.0.0            # Orange Book zip parsing in build_drugs_json.py
requests>=2.31.0         # RxNorm + OpenFDA API calls in build_drugs_json.py

# ── product layer ─────────────────────────────────────────────────────────────
fastapi>=0.110.0         # API backend
uvicorn>=0.29.0          # ASGI server for FastAPI
yfinance>=0.2.40         # live stock price pull at render time
PyYAML>=6.0              # frontmatter parsing in api/main.py
google-cloud-storage>=2.0.0  # GCS blob listing and download in agents/gcs.py